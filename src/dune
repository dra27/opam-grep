(executable
 (name main)
 (public_name opam-grep)
 (libraries
  progress
  cmdliner
  fpath
  bos
  opam-repository))

(rule
 (target config.ml)
 (action
  (with-stdout-to %{target}
   (progn
    (echo "let version = {|%{version:opam-grep}|}\n")))))
